# Documentation index and governance map

1. Product spec — product behavior and MVP scope (`docs/PRODUCT-SPEC.md`).
2. Architecture — components, trust boundaries and deployment (`docs/ARCHITECTURE.md`).
3. WebMCP tool contracts — agent-facing tools and structured errors (`docs/WEBMCP-TOOL-CONTRACTS.md` & `docs/TOOL-CONTRACTS.md`).
4. Consent protocol — proposal/approval state machine (`docs/CONSENT-PROTOCOL.md`).
5. Experience spec — shared canvas and accessibility (`docs/EXPERIENCE-SPEC.md`).
6. Threat model — threats and mitigations (`docs/THREAT-MODEL.md`).
7. Test/evidence/release — verification and release gates (`docs/TEST-EVIDENCE-RELEASE.md` & `docs/HSK-07-RELEASE-EVIDENCE.md`).
8. Rules compliance matrix — contest ownership and proof (`docs/RULES-COMPLIANCE-MATRIX.md`).
9. Implementation decisions — frozen technical choices (`docs/IMPLEMENTATION-DECISIONS.md`).
10. Implementation plan — tranches and coding-agent handoff (`docs/IMPLEMENTATION-PLAN.md`).
11. Submission pack — Devpost copy, tool matrix, verification dossier, and demo script (`docs/SUBMISSION-PACK.md`).
12. Deployment runbook — production deployment, operations, live verification, and release sign-off (`docs/DEPLOYMENT-RUNBOOK.md`).
13. Engineering workbook — Jira-style epics, sprint boards, and task execution logs (`docs/WORKBOOK.md`).

Precedence: implementation decisions → machine-readable contracts → policy/state machine → specifications → code.
