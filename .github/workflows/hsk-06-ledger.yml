name: HSK-06 evidence ledger
on:
  push:
    branches: [hsk-06-protected-actions-evidence]
permissions:
  contents: write
jobs:
  validate-ledger:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { ref: hsk-06-protected-actions-evidence }
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - uses: pnpm/action-setup@v4
        with: { version: 10.15.0 }
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/worker/src/evidence.ts apps/worker/src/protected-runtime.ts tests/evidence.test.ts tests/protected-runtime.test.ts
      - run: pnpm check
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/worker/src tests
          git diff --cached --quiet || (git commit -m 'style(hsk-06): format evidence transaction' && git push)
